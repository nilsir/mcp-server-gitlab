{
  "name": "@nilsir/mcp-server-gitlab",
  "version": "1.0.0",
  "description": "GitLab MCP Server - 通过 MCP 协议与 GitLab API 交互",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-server-gitlab": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest",
    "test:run": "vitest run",
    "test:real": "tsx tests/test-real.ts",
    "lint": "eslint src"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
